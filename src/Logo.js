import React from 'react';
import { Typography } from '@material-ui/core';

export default function Logo() {
  return (
    <div id="logo">
      <Typography variant="h4">Tokemak.radio</Typography>
    </div>
  );
}
